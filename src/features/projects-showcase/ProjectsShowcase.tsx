import { Section } from '../../shared/ui';
import { ProjectAccordion } from './ProjectAccordion';

export const ProjectsShowcase = () => {
  return (
    <Section title="Портфолио проектов">
      <ProjectAccordion />
    </Section>
  );
};
